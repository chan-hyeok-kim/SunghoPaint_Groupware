<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.ham.len.attendance.AttendanceDAO">
	<select id="getStatus" resultType="AttendanceVO">
		SELECT * FROM
			ATTENDANCE
		WHERE YEAR(CURDATE()) = YEAR(ATTENDANCE_DATE) AND MONTH(CURDATE()) = MONTH(ATTENDANCE_DATE)
	</select>
</mapper>